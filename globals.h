#ifndef GLOBALS_H
#define GLOBALS_H

// I will store global variables and objects here. So I should be able to access them from everywhere
enum KeyActions {Left = 1, Right, Crouch, Jump, LA, HA, Block};

#endif // GLOBALS_H
